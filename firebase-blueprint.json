{
  "entities": {
    "users": {
      "title": "Usuários",
      "description": "Dados do usuário/carpinteiro autenticado",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "email": { "type": "string" },
        "displayName": { "type": "string" },
        "empresa": { "type": "string" },
        "telefone": { "type": "string" },
        "createdAt": { "type": "string" }
      },
      "required": ["id"]
    },
    "orcamentos": {
      "title": "Orçamentos",
      "description": "Orçamentos de carpintaria gerados pelo usuário",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "userId": { "type": "string" },
        "numero": { "type": "string" },
        "cliente": { "type": "object" },
        "servico": { "type": "string" },
        "comodos": { "type": "array" },
        "dimensoesGerais": { "type": "object" },
        "materiais": { "type": "array" },
        "maoDeObra": { "type": "object" },
        "totais": { "type": "object" },
        "status": { "type": "string" },
        "observacoes": { "type": "string" },
        "createdAt": { "type": "string" },
        "updatedAt": { "type": "string" }
      },
      "required": ["id", "userId", "numero", "servico"]
    },
    "clientes": {
      "title": "Clientes",
      "description": "Histórico de clientes cadastrados",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "userId": { "type": "string" },
        "nome": { "type": "string" },
        "telefone": { "type": "string" },
        "endereco": { "type": "string" },
        "cidade": { "type": "string" },
        "createdAt": { "type": "string" }
      },
      "required": ["id", "userId", "nome", "telefone"]
    },
    "materiais_padrao": {
      "title": "Materiais Padrão",
      "description": "Tabela de preços e especificações de madeiras da empresa",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "userId": { "type": "string" },
        "categoria": { "type": "string" },
        "nome": { "type": "string" },
        "unidade": { "type": "string" },
        "precoUnitario": { "type": "number" },
        "especificacao": { "type": "string" }
      },
      "required": ["id", "nome", "precoUnitario"]
    },
    "configuracoes": {
      "title": "Configurações Gerais",
      "description": "Configurações da empresa, valores de diária e dados de contato",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "nomeEmpresa": { "type": "string" },
        "telefoneEmpresa": { "type": "string" },
        "chavePix": { "type": "string" },
        "cidade": { "type": "string" },
        "diariaCarpinteiroPadrao": { "type": "number" },
        "diariaAjudantePadrao": { "type": "number" },
        "precoKmDeslocamentoPadrao": { "type": "number" }
      },
      "required": ["userId"]
    }
  },
  "firestoreRules": "rules_version = '2';\nservice cloud.firestore {\n  match /databases/{database}/documents {\n    match /users/{userId} {\n      allow read, write: if request.auth != null && request.auth.uid == userId;\n    }\n    match /orcamentos/{orcamentoId} {\n      allow read, write: if request.auth != null && (resource == null || resource.data.userId == request.auth.uid) && request.resource.data.userId == request.auth.uid;\n    }\n    match /clientes/{clienteId} {\n      allow read, write: if request.auth != null && (resource == null || resource.data.userId == request.auth.uid) && request.resource.data.userId == request.auth.uid;\n    }\n    match /materiais_padrao/{matId} {\n      allow read, write: if request.auth != null && (resource == null || resource.data.userId == request.auth.uid) && request.resource.data.userId == request.auth.uid;\n    }\n    match /configuracoes/{userId} {\n      allow read, write: if request.auth != null && request.auth.uid == userId;\n    }\n  }\n}"
}
